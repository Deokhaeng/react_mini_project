import React from "react";
import Post from "../components/Post";

const Detail = (props) => {
    return (
        <React.Fragment>
            <Post />
        </React.Fragment>
    )
}

export default Detail;